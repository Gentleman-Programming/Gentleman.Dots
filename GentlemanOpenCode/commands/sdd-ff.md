---
description: Fast-forward all planning artifacts
agent: sdd-orchestrator
---

Fast-forward all planning artifacts: propose → spec → design → tasks. $ARGUMENTS

Working directory: `!echo -n "$(pwd)"`
