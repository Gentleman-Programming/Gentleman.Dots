---
description: Continue the next SDD phase for a change
agent: sdd-orchestrator
---

Continue the current change — run the next phase that hasn't been completed yet. $ARGUMENTS

Working directory: `!echo -n "$(pwd)"`
