---
description: Initialize SDD in the current project
agent: sdd-orchestrator
---

Initialize Spec-Driven Development in this project. Detect stack, conventions, and set up persistence mode.

Working directory: `!echo -n "$(pwd)"`
