{ lib, pkgs, ... }:
{
  home.activation.installSketchybar = lib.hm.dag.entryAfter ["writeBoundary"] ''
    echo "═══════════════════════════════════════════════════════════════"
    echo "           Setting up SketchyBar..."
    echo "═══════════════════════════════════════════════════════════════"
    
    # ─────────────────────────────────────────────────────────────────
    # 1. Install SketchyBar via Homebrew
    # ─────────────────────────────────────────────────────────────────
    if ! command -v sketchybar &> /dev/null; then
      echo "📦 Installing SketchyBar via Homebrew..."
      brew tap FelixKratz/formulae 2>/dev/null || true
      brew install sketchybar
    else
      echo "✓ SketchyBar already installed"
    fi
    
    # ─────────────────────────────────────────────────────────────────
    # 2. Create config directory
    # ─────────────────────────────────────────────────────────────────
    SKETCHYBAR_DIR="$HOME/.config/sketchybar"
    mkdir -p "$SKETCHYBAR_DIR/plugins"
    
    # ─────────────────────────────────────────────────────────────────
    # 3. Copy main config
    # ─────────────────────────────────────────────────────────────────
    echo "📋 Copying SketchyBar configuration..."
    cp "${toString ./sketchybar}/sketchybarrc" "$SKETCHYBAR_DIR/sketchybarrc"
    chmod +x "$SKETCHYBAR_DIR/sketchybarrc"
    
    # ─────────────────────────────────────────────────────────────────
    # 4. Copy plugins
    # ─────────────────────────────────────────────────────────────────
    echo "🔌 Copying plugins..."
    cp "${toString ./sketchybar}/plugins/"*.sh "$SKETCHYBAR_DIR/plugins/"
    chmod +x "$SKETCHYBAR_DIR/plugins/"*.sh
    
    # ─────────────────────────────────────────────────────────────────
    # 5. Start SketchyBar via brew services (auto-start on login)
    # ─────────────────────────────────────────────────────────────────
    echo "🚀 Starting SketchyBar..."
    if pgrep -x "sketchybar" > /dev/null; then
      sketchybar --reload
      echo "   ✓ SketchyBar reloaded"
    else
      brew services start sketchybar 2>/dev/null || true
      echo "   ✓ SketchyBar started"
    fi
    
    echo ""
    echo "═══════════════════════════════════════════════════════════════"
    echo "           ✅ SketchyBar setup complete!"
    echo "═══════════════════════════════════════════════════════════════"
    echo ""
  '';
}
